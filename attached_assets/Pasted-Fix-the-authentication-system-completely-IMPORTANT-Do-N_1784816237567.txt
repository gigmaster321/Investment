Fix the authentication system completely.

IMPORTANT:
Do NOT redesign the UI.
Do NOT change any existing styling.
Do NOT implement Email OTP, SMTP, Resend, or any production email features.
Focus only on making the authentication work reliably.

=========================
1. USER REGISTRATION
=========================

- Fix the registration form.
- Registration must always succeed when valid information is entered.
- Save every registered user permanently in the application's database/storage.
- The user data must NOT disappear after refresh or restart.
- Prevent duplicate email registrations.
- Store:
  - Full Name
  - Username
  - Email
  - Phone
  - Password (hashed)
  - Balance (default 0)
  - Plan (None)
  - Status (Active)
  - Created Date

After successful registration:
- Show a success message.
- Redirect to the Login page.

=========================
2. USER LOGIN
=========================

Completely fix the login system.

- Remove all current login errors.
- A registered user must always be able to log in using the same email and password used during registration.
- If credentials are wrong, show a proper error.
- If credentials are correct, login must always succeed.
- Keep the user session active until logout.
- After login redirect to the User Dashboard.

=========================
3. ADMIN LOGIN
=========================

Create ONE permanent administrator account.

Admin Email:
admin@quantuminvestments.com

Admin Password:
Admin@123

Requirements:

- Only this email/password can access the Admin Dashboard.
- No registration for admin.
- No Forgot Password.
- No OTP.
- No email verification.
- Remove every admin login error.
- After successful login redirect directly to Admin Dashboard.

=========================
4. SESSION
=========================

- User remains logged in until Logout.
- Admin remains logged in until Logout.
- Refreshing the page must NOT log the user out.
- Authentication state must persist correctly.

=========================
5. DATABASE
=========================

Fix every authentication/database issue.

Ensure:

- Registration saves correctly.
- Login reads the saved user correctly.
- No INTERNAL_ERROR.
- No DATABASE_ERROR.
- No LOGIN_FAILED errors.
- No REGISTER_FAILED errors.

=========================
6. FINAL REQUIREMENT
=========================

Before finishing:

- Test User Registration.
- Test User Login.
- Test Admin Login.
- Test Logout.
- Test Refresh.

Verify all of them work successfully.

Do not leave any authentication errors.

Only finish when:
- Registration works.
- User Login works.
- Admin Login works.
- Sessions persist correctly.
- No authentication errors remain.